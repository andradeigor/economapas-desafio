import { Card } from "components/Card/Card";
import { TicketContainer } from "./style";

export const Main: React.FC = () => {
  return (
    <TicketContainer>
      <Card></Card>
    </TicketContainer>
  );
};
